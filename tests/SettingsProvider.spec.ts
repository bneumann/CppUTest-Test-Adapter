// TODO: Cannot test with dependencies to vscode API
// import * as vscode from 'vscode';
// import { instance, mock } from "ts-mockito";
// import { SettingsProvider } from './SettingsProvider';

// describe("SettingsProvider.spec should", () => {
//   it("return all executables", () => {
//     const configMock = mock<vscode.WorkspaceConfiguration>();

//     const settingsProvider = new SettingsProvider(instance(configMock));

//     settingsProvider.GetTestPath().should.be.eq("/mnt/myPath");
//   })
// });